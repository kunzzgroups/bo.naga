(()=>{'use strict';
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),money=v=>Number(v||0).toLocaleString('en-MY',{minimumFractionDigits:2,maximumFractionDigits:2});
function fmt(d){const p=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}`}function addDay(v){const a=String(v||'').split('-').map(Number);return new Date(Date.UTC(a[0],a[1]-1,a[2]+1)).toISOString().slice(0,10)}
async function api(path,opt={}){const headers={...BO_AUTH.authHeader(),...(opt.body?{'Content-Type':'application/json'}:{})};const r=await fetch(API_CONFIG.BASE_URL+path,{...opt,headers,cache:'no-store'}),j=await r.json().catch(()=>({}));if(!r.ok||j.status==='error')throw Error(j.message||'Request failed');return j.data??j}
function initDates(){const now=new Date(),first=new Date(now.getFullYear(),now.getMonth(),1),today=fmt(now);$('mpDate').value=today;$('mpFrom').value=fmt(first);$('mpTo').value=today}
async function load(){try{const data=await api(`/admin/main/merchant-profit?from=${encodeURIComponent($('mpFrom').value)}&to=${encodeURIComponent(addDay($('mpTo').value))}`);$('mpTotal').textContent=money(data.total);const rows=data.rows||[];$('mpRows').innerHTML=rows.length?rows.map(x=>`<tr><td>${esc(x.incomeDate)}</td><td><b>${esc(x.source)}</b></td><td>${esc(x.description||'—')}</td><td>${esc(x.createdBy||'—')}</td><td>${esc(String(x.createdAt||'').replace('T',' ').slice(0,19))}</td><td class="num amount">${money(x.amount)}</td><td class="num"><button class="clean-btn" data-delete-id="${x.id}" title="Delete"><i class="bi bi-trash"></i></button></td></tr>`).join(''):'<tr><td colspan="7" class="mp-empty">No merchant income records for selected period.</td></tr>'}catch(e){$('mpRows').innerHTML=`<tr><td colspan="7" class="mp-empty text-danger">${esc(e.message)}</td></tr>`}}
$('merchantProfitForm').addEventListener('submit',async e=>{e.preventDefault();const msg=$('mpMessage');msg.className='small mt-2 text-muted';msg.textContent='Saving...';try{await api('/admin/main/merchant-profit',{method:'POST',body:JSON.stringify({incomeDate:$('mpDate').value,source:$('mpSource').value,amount:$('mpAmount').value,description:$('mpDescription').value})});msg.className='small mt-2 text-success';msg.textContent='Merchant income added successfully.';$('mpSource').value='';$('mpAmount').value='';$('mpDescription').value='';await load()}catch(x){msg.className='small mt-2 text-danger';msg.textContent=x.message}});
$('mpSearch').onclick=load;document.addEventListener('click',async e=>{const b=e.target.closest('[data-delete-id]');if(!b)return;const ok=window.BO_DIALOG?.confirm?await BO_DIALOG.confirm('Delete this merchant income record? This will immediately update dashboard profit totals.',{title:'Delete Merchant Income',confirmText:'Delete',type:'danger'}):false;if(!ok)return;try{await api('/admin/main/merchant-profit/'+b.dataset.deleteId,{method:'DELETE'});await load()}catch(x){window.BO_DIALOG?.alert?BO_DIALOG.alert(x.message,{title:'Unable to Delete',type:'error'}):alert(x.message)}});
BO_AUTH.requireLogin();initDates();load();
})();
