(function(){'use strict';const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),money=v=>Number(v||0).toLocaleString('en-MY',{minimumFractionDigits:2,maximumFractionDigits:2}),num=v=>Number(v||0).toLocaleString('en-US');const API=String(API_CONFIG.BASE_URL||'').replace(/\/+$/,'');function url(p){if(/\/api$/i.test(API)&&/^\/api\//i.test(p))p=p.replace(/^\/api/i,'');return API+p}async function req(p){const r=await fetch(url(p),{headers:{...BO_AUTH.authHeader()}}),j=await r.json().catch(()=>({}));if(!r.ok||j.status==='error')throw Error(j.message||'Request failed');return j.data}function card(icon,label,value,small){return `<div class="perf-kpi"><span class="perf-kpi-icon"><i class="bi ${icon}"></i></span><div><span>${esc(label)}</span><strong>${esc(value)}</strong><small>${esc(small||'Selected period')}</small></div></div>`}function dt(v){return String(v||'').replace('T',' ').slice(0,19)||'-'}async function init(){BO_AUTH.requireLogin();await BO_AUTH.refreshMe();const p=new URLSearchParams(location.search),id=p.get('agentId'),brand=p.get('brandId')||'',from=p.get('from')||new Date().toISOString().slice(0,10),to=p.get('to')||from;if(!id){location.href='agent-performance-report.html';return}const qs=new URLSearchParams({agentId:id,from,to});if(brand)qs.set('brandId',brand);const [r,b]=await Promise.all([req('/api/admin/reports/agent-performance?'+qs),req('/api/admin/reports/agent-performance/'+encodeURIComponent(id)+'/bets?'+new URLSearchParams({...(brand?{brandId:brand}:{}),from,to}))]);const a=(r.rows||[])[0]||{};$('detailName').textContent=`${a.agentCode||('Agent #'+id)} · ${a.agentName||''}`;$('detailSubtitle').textContent=`${r.brand?.brandName||''} · ${from} - ${to}`;$('detailRange').textContent=`${from} - ${to}`;$('detailKpis').innerHTML=card('bi-people','Players',num(a.totalPlayers),'Assigned')+card('bi-coin','Turnover','RM '+money(a.turnover),'Valid bet')+card('bi-graph-up-arrow','House Profit','RM '+money(a.houseProfit),'Player loss + / win -')+card('bi-cash-stack','Deposit','RM '+money(a.depositAmount),num(a.depositCount)+' tx')+card('bi-cash-coin','Withdraw','RM '+money(a.withdrawAmount),num(a.withdrawCount)+' tx')+card('bi-gift','Bonus / Settlement','RM '+money(Number(a.bonusAmount||0)+Number(a.settlementAmount||0)),`Bonus ${money(a.bonusAmount)} · Settlement ${money(a.settlementAmount)}`);$('detailOps').innerHTML=[['Deposit','RM '+money(a.depositAmount),num(a.depositCount)+' transaction(s)'],['Withdraw','RM '+money(a.withdrawAmount),num(a.withdrawCount)+' transaction(s)'],['Bonus Given','RM '+money(a.bonusAmount),'Agent players'],['Player Win','RM '+money(a.playerWin),'Players won'],['Player Loss','RM '+money(a.playerLoss),'Players lost'],['Settlement Paid','RM '+money(a.settlementAmount),'Agent settlement'],['Pending Settlement','RM '+money(a.pendingSettlement),'Awaiting processing'],['Available Balance','RM '+money(a.availableBalance),'Agent wallet']].map(x=>`<div class="perf-op"><span>${x[0]}</span><strong>${x[1]}</strong><small>${x[2]}</small></div>`).join('');const rows=b.rows||[];$('detailRows').innerHTML=rows.map(x=>`<tr><td>${esc(dt(x.settlementAt))}</td><td><b>${esc(x.playerName||x.playerId)}</b><small class="d-block">#${esc(x.playerId)}</small></td><td>${esc(x.providerCode||'-')}</td><td>${esc(x.gameName||x.gameCode||'-')}</td><td>RM ${money(x.totalBet)}</td><td>RM ${money(x.validBet)}</td><td>RM ${money(x.totalWin)}</td><td class="${Number(x.playerPL||0)>=0?'perf-pos':'perf-neg'}">RM ${money(x.playerPL)}</td></tr>`).join('')||'<tr><td colspan="8" class="perf-empty">No betting records for this period.</td></tr>';$('detailShowing').textContent=`Showing ${rows.length} betting record${rows.length===1?'':'s'}`;$('detailBack').onclick=()=>history.length>1?history.back():location.href='agent-performance-report.html'}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>init().catch(e=>BO_DIALOG.alert(e.message,{title:'Agent Performance',type:'error'})));else init().catch(e=>BO_DIALOG.alert(e.message,{title:'Agent Performance',type:'error'}));})();
