# Shell scripts must stay LF. A hook script checked out with CRLF fails on Linux
# clones with a "bad interpreter" error, which would silently disable the guard.
scripts/git-hooks/* text eol=lf
*.sh text eol=lf
